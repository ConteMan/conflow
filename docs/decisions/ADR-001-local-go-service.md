# ADR-001：Go 单二进制与本地 Web 服务

> 状态：已接受（2026-07-10）

## 决策

Conflow 使用 Go 构建单一二进制。`serve` 在 loopback 地址启动 HTTP 服务并托管嵌入式 Web UI；CLI 和 GUI 共用同一 Go 领域内核。

## 理由

- PM 可在浏览器完成日常流程，不必安装桌面壳。
- CLI 可以被 Git、CI 和自动化调用。
- 发布物不依赖 Node 或常驻云后端。

## 后果

- 前端必须在构建期嵌入二进制。
- 本地 HTTP 安全边界和跨平台浏览器打开行为属于产品责任。
